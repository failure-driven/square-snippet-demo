<ul class="nav nav-tabs mb-3">
  <li class="nav-item">
    <%= link_to(
        "Status",
        identity_site_path(identity_id: @identity.uid, id: @site.reference_id),
        aria: { current: :page },
        class: "nav-link #{action_name.to_sym == :show ? "active" : ""}"
    ) %>
  </li>
  <li class="nav-item">
    <%= link_to(
        "Config",
        configure_site_config_identity_site_path(identity_id: @identity.uid, id: @site.reference_id),
        aria: { current: :page },
        class: "nav-link #{action_name.to_sym == :configure_site_config ? "active" : ""}"
    ) %>
  </li>
  <li class="nav-item">
    <%= link_to(
        "Stats",
        stats_identity_site_path(identity_id: @identity.uid, id: @site.reference_id),
        aria: { current: :page },
        class: "nav-link #{action_name.to_sym == :stats ? "active" : ""}"
    ) %>
  </li>
  <li class="nav-item">
    <%= link_to(
        "Stories",
        stories_identity_site_path(identity_id: @identity.uid, id: @site.reference_id),
        aria: { current: :page },
        class: "nav-link #{action_name.to_sym == :stories ? "active" : ""}"
    ) %>
  </li>
  <% if Flipper[:portal].enabled?(FormUser.find(@identity.user_id))%>
    <li class="nav-item">
      <%= link_to(
          "Portal",
          demo_portal_identity_site_path(identity_id: @identity.uid, id: @site.reference_id),
          aria: { current: :page },
          class: "nav-link #{action_name.to_sym == :demo_portal ? "active" : ""}"
      ) %>
    </li>
  <% end %>
</ul>